"""
    小结 - 字典常用操作
        创建
            字典名 = {键1:值,键2:值}
            字典名 = dict(可迭代对象)
        添加
            字典名[键] = 值
        定位
            字典名[键] = 值 # 修改字典值
            值 = 字典名[键] # 读取字典值
        遍历
            for key in 字典名:
            for value in 字典名.values():
            for k,v in 字典名.items():
        删除
            del 字典名[键]
    列表常用操作:day05/demo03
"""

